
TARGETPATH=obj
TARGETTYPE=LIBRARY

!include $(ANI_WINHDD_ROOT_DIR)\sources.common

!include $(ANI_WINHDD_ROOT_DIR)\sources.fw

!include $(ANI_MAC_SOURCE_ROOT_DIR)\include\sources.includes

# The C_DEFINES and RCOPTIONS macros are only appropriate on a Windows-based desktop platform.
# Use the CDEFINES and RDEFINES macros instead.
#!if "$(AniBldOs)" == "wince"
CDEFINES=$(C_DEFINES)
RDEFINES=$(RCOPTIONS)
RELEASETYPE=$(AniRelStr)
WINCEOEM=$(AniOemType)
WINCEREL=$(AniRelType)
#!endif


